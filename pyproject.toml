[project]
name = "smb-duckdb-lakehouse-lab"
version = "0.1.0"
description = "Build-in-public lab: a cheap, cloud-agnostic DuckDB lakehouse for SMB"
requires-python = ">=3.11"
dependencies = [
    "duckdb>=1.1",
    "polars>=1.0",
    "pyarrow>=16",
]
